{% extends 'base.html.twig' %}

{% block title %}ArticleComment{% endblock %}

{% block body %}
    <h1>ArticleComment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_comment.id }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ article_comment.author }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ article_comment.content }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ article_comment.createdAt ? article_comment.createdAt|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('article_comment_index') }}">back to list</a>

    <a href="{{ path('article_comment_edit', {'id': article_comment.id}) }}">edit</a>

    {{ include('article_comment/_delete_form.html.twig') }}
{% endblock %}
